package com.example.githubtestinterview.ViewModel

import androidx.lifecycle.ViewModel

class SearchViewModel : ViewModel() {
    // Define your Retrofit services here

    // Define LiveData objects for Users and Repositories

    fun search(query: String) {
        // Make network calls with Retrofit services, collect results
        // and post them to LiveData
    }
}
